package org.roboquant.common;

/**
 * Signal type representing the direction of a signal: ENTRY, EXIT, or BOTH.
 * Used by more advanced strategies that provide different signals for entering and exiting a position.
 */
public enum SignalType {

    ENTRY, EXIT, BOTH

}
